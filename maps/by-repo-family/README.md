# By Repo Family

Routes sessions by broad family such as `aoa`, `tree-of-sophia`, `abyss`,
`codex-memory`, or external work.

Generated entries belong in `entries/` and should derive from work-context
classification, repo names, and route-card evidence.
