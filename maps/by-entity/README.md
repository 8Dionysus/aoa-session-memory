# By Entity

Routes sessions by named objects such as repos, skills, hooks, MCP resources,
models, packages, commands, issues, PRs, config keys, or environment variables.

Generated entries belong in `entries/`.
