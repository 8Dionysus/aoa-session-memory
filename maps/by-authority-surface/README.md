# By Authority Surface

Routes sessions by authority layer: source, generated, runtime, diagnostics,
portable bundle, local overlay, memory, or external snapshot.

Generated entries belong in `entries/`.
