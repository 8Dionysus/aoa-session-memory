# By Work Context

Routes sessions by likely active workspace or repository root.

Generated entries belong in `entries/` and should derive from `work_context`,
`cwd`, path evidence, and project grounding.
